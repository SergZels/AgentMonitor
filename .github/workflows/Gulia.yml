name: 🚀 Deploy to Server Gulia

on:
  workflow_dispatch:  # Можливість запуску вручну лише через GitHub UI

env:
  DEPLOY_PATH: "/home/TMP2"

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🔐 Hello World
      run: |
        echo "hello world"

