name: 🚀 Deploy to Server Gulia

on:
  push:
    branches: [ Gulia ]
  workflow_dispatch:  # Можливість запуску вручну

env:
  DEPLOY_PATH: "/home/TMP2"  # Змініть на ваш шлях

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: 🔐 Hello World
      run: |
      
        echo "hello world"
